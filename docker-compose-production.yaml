services:
  web:
    build: .
    container_name: mone_be
    restart: unless-stopped
    ports:
      - "127.0.0.1:3011:8080"
    env_file:
      - .env
    networks:
      - mariadb_network

networks:
  mariadb_network:
    external: true
